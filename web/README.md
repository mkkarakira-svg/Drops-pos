# Drops POS Web/PWA
Mobile-first offline-capable POS for Drops Detergents.

Open `web/index.html` locally for development. For production, serve the `web/` directory over HTTPS so PWA install/service worker works.
